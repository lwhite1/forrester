/*
 * Copyright (c) 2026 Courant Systems
 * Licensed under CC-BY-SA-4.0. See LICENSE in this module for details.
 */
package systems.courant.sd.demo.userguidechap06complete;

import systems.courant.sd.Simulation;
import systems.courant.sd.model.ModelMetadata;
import systems.courant.sd.model.compile.ModelCompiler;
import systems.courant.sd.model.def.ModelDefinitionBuilder;
import systems.courant.sd.model.def.VariableDef;
import systems.courant.sd.model.def.FlowDef;
import systems.courant.sd.model.def.StockDef;

/**
 * Imported from: POP.MDL
 * Source: Vensim User Guide Models, bundled with Vensim (https://vensim.com). See LICENSING.md for project license details.
 * License: MIT
 * Author: Ventana Systems, Inc.
 *
 * <p>Auto-generated by courant-tools ImportPipeline.
 *
 * <p>Validation errors (model may need manual fixes):
 * <ul>
 *   <li>View 'View 1' places non-existent element: _48</li>
 *   <li>View 'View 1' places non-existent element: _48</li>
 *   <li>View 'View 1' flow route references non-existent flow: _48</li>
 *   <li>View 'View 1' flow route references non-existent flow: _48</li>
 * </ul>
 */
public class VensimPop {

    public static void main(String[] args) {
        new VensimPop().run();
    }

    public void run() {
        var builder = new ModelDefinitionBuilder()
                .name("POP")
                .defaultSimulation("Year", 30.0, "Year", 0.125);

        // Stocks
        builder.stock(new StockDef("Rabbit Population", "Rabbit Population", 1000.0, "rabbit", null));

        // Constants
        builder.constant("average lifetime", 8.0, "Year");
        builder.constant("birth rate", 0.125, "fraction/Year");

        // Variables
        builder.variable(new VariableDef("births", "births", "Rabbit_Population * birth_rate", "rabbit/Year"));
        builder.variable(new VariableDef("deaths", "deaths", "Rabbit_Population / average_lifetime", "rabbit/Year"));

        // Flows
        builder.flow(new FlowDef("Rabbit Population inflow 1", null, "births", "Year", null, "Rabbit Population"));
        builder.flow(new FlowDef("Rabbit Population outflow 2", null, "deaths", "Year", "Rabbit Population", null));

        var definition = builder.build();
        var compiled = new ModelCompiler().compile(definition);

        compiled.getModel().setMetadata(ModelMetadata.builder()
                .author("Ventana Systems, Inc.")
                .source("Vensim User Guide Models, bundled with Vensim (https://vensim.com). See LICENSING.md for project license details.")
                .license("MIT")
                .url("https://www.vensim.com/documentation/models.html")
                .build());

        Simulation sim = compiled.createSimulation();
        sim.execute();
    }
}
